document.addEventListener("DOMContentLoaded", () => {
console.log("FlipAI app.js loaded");

// This file will hold ALL frontend logic moving forward.
// For now, we are only confirming it loads safely.
});
